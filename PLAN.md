# Implementation Plan

## 현재 단계

Phase 2 원본 데이터 프로파일링과 Phase 3의 환경 사망 정제 수직 슬라이스를
완료했다. 다음은 정제 Parquet을 입력으로 공간 집중 지표를 구현하는 단계다.

현재 작업:

> 맵·사망 원인·공간 격자별 사망 수와 고유 경기·날짜 수를 계산해 반복
> 집중 위치를 식별한다.

Phase 3의 첫 구현으로 DuckDB가 deaths CSV 5개에서 `Falling`과 `Drown`을
조회하고 aggregate CSV 5개의 기존 날짜를 `match_id`로 연결하도록 작성했다.
2026-07-31 전체 입력 실행에서 원본 후보 980,079행, 최종 분석 가능
793,356행을 재현했고 `data/processed/environmental_deaths.parquet` 37.58 MB를
19.17초에 생성했다.

현재 분석 대상 컬럼:

```text
killed_by
map
match_id
victim_position_x
victim_position_y
date
```

`date`는 deaths에 없으므로 aggregate에서 `match_id`로 연결한다.

## 완료

- [x] Kaggle 데이터셋 접근 상태와 공개 메타데이터 조사
- [x] 라이선스와 원본 재배포 위험 조사
- [x] 단계별 분석 운영 프로세스 문서 작성
- [x] `main`과 `dev` 브랜치 초기화
- [x] Python 프로젝트 구조 작성
- [x] 데이터와 비밀값 Git 제외 정책 작성
- [x] 로컬 deaths CSV 5개와 aggregate CSV 5개 확인
- [x] deaths 전체 65,370,475행과 722,425개 고유 경기 측정
- [x] 환경 사망 문제 정의와 초기 필요 컬럼 선정
- [x] DuckDB 기반 deaths 1차 프로파일러 구현 및 전체 데이터 실행
- [x] 프로젝트 용어집 작성
- [x] 현재 환경 사망 분석 범위의 프로파일링 완료
- [x] 최종 분석 가능 793,356행과 건수 대조 차이 0 확인
- [x] 전체 `killed_by` 값을 검토하고 메인 범위를 지형 기반 `Falling`·`Drown`으로 확정
- [x] 환경 사망 정제 파이프라인과 Parquet 출력 전체 데이터 실행

## Phase 1. Source intake

- [ ] Python 3.12 버전과 전체 개발 도구 설치 상태 기록
- [ ] Kaggle CLI 인증 상태 기록
- [ ] 다운로드 전후 디스크 공간 기록
- [ ] 원본 압축 파일의 저장 위치와 파일명 기록
- [ ] 실제 압축 파일 크기 기록
- [ ] 파일 체크섬 기록
- [x] 압축 해제된 aggregate·deaths 파일 수 확인
- [x] 로컬 Erangel·Miramar 지도 이미지와 해상도 확인
- [ ] 지도 이미지의 원 출처, 별도 사용 조건과 정확한 버전 확인
- [x] 현재 분석 범위를 deaths와 aggregate의 필요 컬럼으로 제한

## Phase 2. Profiling

### 완료

- [x] deaths 파일별 행 수 측정
- [x] deaths 전체 행 수와 고유 `match_id` 수 측정
- [x] 핵심 컬럼의 NULL과 빈 문자열 측정
- [x] 피해자 좌표 숫자 변환 가능 여부 측정
- [x] 맵별 행 수와 고유 경기 수 측정
- [x] `killed_by` 전체 값 분포 측정
- [x] `Falling`과 `Drown` 행 수 측정
- [x] 맵별 좌표 최솟값과 최댓값 측정
- [x] `0,0`, 음수, 비유한 좌표 측정
- [x] 공식 문서를 근거로 에란겔·미라마 좌표 범위 `0~816,000` 확정
- [x] 정상 좌표 범위 밖 71행을 맵·환경 사망 원인별로 측정
- [x] `Drown`의 `0,0` 패턴을 맵·파일·경기·시간 기준으로 조사
- [x] `Falling`과 `Drown`의 비영점 좌표 커버리지 측정
- [x] deaths와 aggregate의 날짜 연결률 측정
- [x] aggregate에 기록된 경기 시각 범위 측정
- [x] 프로파일링 실행 시간 측정

### 진행 예정

- [x] deaths의 완전 중복과 이벤트 중복 후보 측정
- [x] 동일 `match_id`의 맵 일관성과 결측 맵의 경기 내 복구 가능성 확인
- [x] 여섯 개 분석 컬럼과 모든 품질 규칙을 적용한 최종 후보 행 수 측정
- [x] deaths·aggregate 전체 파일 간 스키마와 타입 일치 확인
- [x] 현재 분석 컬럼 기준 데이터 사전 초안 작성

### Phase 2 종료 기준

다음 두 항목을 완료해 현재 분석 목적의 프로파일링을 종료했다.

1. deaths·aggregate 각 5개의 컬럼과 타입 일관성 확인 완료
2. 모든 규칙을 적용한 최종 분석 가능 793,356행과 제외 사유별 고유 행 수
   측정 완료

좌표 백분위, 상세값이 다른 이벤트 후보의 최종 판정과 추가 분포 탐색은 현재
질문에 필요해질 때만 수행한다. 프로파일링 결과의 구조화된 저장은 처리 및
품질 실행 이력 구현 단계에서 진행한다.

## Phase 3. Processing

- [x] DuckDB로 환경 사망 원본 후보 980,079행 재현
- [x] 환경 사망 원본 후보 임시 테이블 생성
- [x] 12개 원본 컬럼 완전 중복 제거 후 980,014행 재현
- [ ] 신규 파일 탐색과 중복 처리 방지
- [ ] 프로파일링·품질 결과를 구조화된 파일 또는 PostgreSQL에 저장
- [x] 환경 사망 분석용 컬럼 선택
- [x] 맵 문자열 정규화와 환경 사망 원인 제한
- [x] 분석 가능 여부와 대표 제외 사유 컬럼 생성
- [ ] 전체 환경 사망과 모든 품질 플래그를 보존하는 감사용 뷰 생성
- [x] 품질 규칙을 통과한 지표용 임시 테이블 생성
- [x] 지표용 결과가 기준선 793,356행과 일치하는지 대조
- [ ] Parquet 변환 필요성과 효과 측정
- [x] 입력·출력·제외 행 수와 실행 시간 콘솔 기록
- [ ] 품질 규칙 미통과 데이터 격리

## Phase 4. Data quality

- [ ] 실제 데이터 기반 품질 규칙 확정
- [x] `map` 결측을 원본에 보존하고 맵 기반 분석에서 제외하는 규칙 확정
- [x] 환경 사망의 `0,0`을 원본에 보존하고 공간 분석에서 제외하는 규칙 확정
- [x] 좌표 범위 밖 값을 원본에 보존하고 공간 분석에서 제외하는 규칙 확정
- [x] 날짜를 생성하지 않고 기존 `match_id`가 연결된 행만 날짜 기반 분석에 사용하는 규칙 확정
- [x] 12개 원본 컬럼 완전 중복을 원본에 보존하고 분석 출력에서 제외하는 규칙 확정
- [ ] 상세값이 다른 사망 이벤트 중복 후보 처리 규칙 확정
- [ ] `PASS`, `WARNING`, `FAIL` 판정 구현
- [ ] 핵심 규칙 pytest 작성
- [ ] 배치 데이터량 이상 탐지

## Phase 5. Metrics and storage

- [x] 100m·250m·500m 격자로 공간 집중 분석 타당성 예비 확인
- [ ] 좌표 격자 크기와 생성 방식 확정
- [ ] 과거 지도 이미지와 게임 좌표의 방향·여백·스케일 정렬 검증
- [ ] 상위 핫스팟의 과거 지도 확대 이미지 생성
- [ ] 맵·격자·사망 원인별 건수 SQL 구현
- [ ] 고유 경기 수와 고유 날짜 수 SQL 구현
- [ ] 환경 사망 집중 지역 선정 기준 확정
- [ ] 데이터 운영 지표 SQL 구현
- [ ] PostgreSQL 결과 스키마 구현
- [ ] 멱등 적재 구현

## Phase 6. BI and reporting

- [ ] Superset 환경 사망 집중 지역 대시보드
- [ ] Superset 데이터 운영 대시보드
- [ ] 규칙 기반 운영 보고서
- [ ] AI 운영 보고서 초안 생성
- [ ] LLM 실패 대체 경로

## 바로 다음 작업

1. 좌표 단위와 지도 정렬 기준을 확인하고 공간 격자 크기를 확정한다.
2. 맵·사망 원인·격자별 사망 건수 SQL을 작성한다.
3. 격자별 고유 경기 수와 고유 날짜 수를 계산한다.
4. 여러 경기와 날짜에 반복되는 hotspot 후보 기준을 정의한다.
5. CSV와 Parquet의 조회 시간을 비교해 변환 효과를 측정한다.
6. 지표 결과와 실행 이력을 구조화해 PostgreSQL 적재 준비를 한다.

## 후속 분석 아이디어

현재 범위에 포함하지 않은 후속 후보:

- [ ] 무기별 킬 거리 분포 측정
- [ ] 무기별 극단 거리 기준과 최소 표본 수 설계
- [ ] 여러 경기에서 반복되는 비정상 킬 거리 행동 후보 탐지
- [ ] aggregate의 `player_kills`와 deaths 기반 킬 수 교차 확인
- [ ] 사용자 이름을 해시한 비식별 후보 ID 설계

이 분석은 핵 사용자를 확정하거나 제재하기 위한 기능이 아니다. 현재 데이터에는
발사·명중·시야·반동·이동 경로·신고·제재 라벨이 없으므로, 구현하더라도 사람의
추가 검토가 필요한 `의심 행동 후보`만 생성한다.

환경 사망 공간 지표와 운영 자동화를 완료하기 전에는 이 아이디어로 현재
프로젝트 범위를 확장하지 않는다.

## 완료 기준

- 실제 샘플과 전체 처리 범위를 구분해 기록한다.
- 처리한 파일과 행 수를 공개 전체 규모와 구분한다.
- pytest와 Ruff가 통과한다.
- 실행 명령과 측정 결과가 README에 기록된다.
- 구현하지 않은 기능을 완료로 표시하지 않는다.

마지막 상태 갱신: 2026-07-31
